{
  "kind": "collectionType",
  "collectionName": "flashcards",
  "info": {
    "name": "flashcard"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "title": {
      "type": "string"
    },
    "content": {
      "type": "richtext"
    },
    "decks": {
      "via": "flashcards",
      "collection": "decks"
    },
    "tags": {
      "collection": "tags",
      "via": "flashcards"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "pending",
        "publish"
      ],
      "default": "pending",
      "required": true
    }
  }
}
