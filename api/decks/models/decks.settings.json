{
  "kind": "collectionType",
  "collectionName": "decks",
  "info": {
    "name": "Decks"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "Title": {
      "type": "string"
    },
    "flashcards": {
      "via": "decks",
      "collection": "flashcard",
      "dominant": true
    },
    "tags": {
      "collection": "tags",
      "via": "decks"
    },
    "users": {
      "plugin": "users-permissions",
      "collection": "user",
      "via": "decks"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "pending",
        "publish"
      ],
      "default": "pending",
      "required": true
    }
  }
}
